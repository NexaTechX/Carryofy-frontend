{
  "name": "carryofy-frontend",
  "version": "0.5.0",
  "private": true,
  "scripts": {
    "build": "cd web && npm install && npm run build",
    "vercel-build": "cd web && npm install && npm run build"
  },
  "dependencies": {
    "next": "16.0.1",
    "react": "19.2.0",
    "react-dom": "19.2.0"
  }
}

